# -*- coding: utf-8 -*-
# Plugin: Beat counter (legacy)
#
# Kept as a no-op so plugin load order/config stays stable while avoiding
# duplicate BAR/BEAT/TICK overlays. Transport timing is shown by timeclock.py.

def draw():
    return
